# Hero Bikestore — Custom Keycloak Login Theme
# Extends keycloak.v2 (Keycloak 26.x PatternFly v5 base)
parent=keycloak.v2
import=common/keycloak

# NOTE: We do NOT set "styles=" here.
# Parent styles are inherited automatically.
# Our CSS is injected via our template.ftl override (see template.ftl).
