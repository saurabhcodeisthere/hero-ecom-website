# Bootstrap config — only what's needed BEFORE connecting to config server
# Everything else (datasource, jpa, rabbitmq) lives in config-server/config-repo/payment-service.yaml
spring:
  application:
    name: payment-service
  config:
    import: "configserver:http://config-server:8888"

server:
  port: 8086
